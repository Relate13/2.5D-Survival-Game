using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;

public class SaveTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*
        EntityControlUnit test = new EntityControlUnit();
        test.entityId = 1;
        test.chunkID = new Vector2Int(0, 0);
        test.inChunkPos = new Vector3(0, 0, 0);
        test.data = "helloworld";
        EntityControlUnit corn1=new EntityControlUnit();
        corn1.entityId = 2;
        corn1.inChunkPos = new Vector3(0, 1, 0);
        corn1.chunkID = new Vector2Int(0, 0);
        corn1.data = "I'm a corn plant";
        Chunk testChunk = new Chunk();
        testChunk.Entities.AddFirst(corn1);
        //testChunk.Entities.AddFirst(test);
        testChunk.Save();
        */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
